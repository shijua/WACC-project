# a simple recursive function

# Output:

# Program:

begin
  val rec(val x) is
    if x == 0
    then
      skip
    else
      val y = call rec(x - 1)
    fi ;
    return 42
  end

  val x = 0 ;
  x = call rec(8)
end
