# program has function which only contains an if statement and a return in each branch

# Output:
# true
#

# Program:

begin
  val f() is
    if true then
      return true
    else
      return false
    fi
  end
  val x = call f();
  println x
end
