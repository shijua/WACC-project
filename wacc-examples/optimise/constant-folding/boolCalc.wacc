# Illustrating Constant Folding Optimisations
# For Boolean Calculation and Shortcutting

# Output:
# false
# true
#

# Program:



begin
    bool x = (0 == 1) && (2 + 3 == 5);
    println x;
    bool y = (3 < 5) || (4 - 1 == 3);
    println y;
    skip
end