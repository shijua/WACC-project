# a simple increment function definition and usage

# Output:
# 1
# 4
#

# Program:

begin
  val inc(val x) is
    return x + 1
  end
  int x = 0 ;
  x = call inc(x) ;
  println x ;
  x = call inc(x) ;
  x = call inc(x) ;
  x = call inc(x) ;
  println x
end
