# Functions should be able to have the same name as variables
# Thanks to Jordan Hall

# Output:
# 0
#

begin
    val inc(val x) is
        return x + 1
    end
    val inc = 0;
    val tmp = call inc(inc);
    println inc
end
